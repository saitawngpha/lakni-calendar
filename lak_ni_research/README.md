# Algorithm notes moved

The maintained English research guide is the repository-level
[`README.md`](../README.md). One source of truth keeps the Ahom, Shan,
Chinese-comparative, and Myanmar rules from drifting apart.

```bash
python3 lak_ni.py --test
python3 lak_jeng.py --test
python3 sakkaraj.py --test
python3 -m unittest discover -s ../tests -v
```
